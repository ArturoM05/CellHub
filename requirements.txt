# Backend (Python / Django) — instalar con: pip install -r requirements.txt
Django>=4.2
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-filter>=23.0
django-cors-headers>=4.0
drf-spectacular>=0.26
Pillow>=10.0
python-decouple>=3.8

# Background tasks
celery>=5.3
redis>=4.5

# Frontend (React + Shopify Polaris) — NO van aquí.
# Dependencias en frontend/package.json — instalar con:
#   cd frontend && npm install && npm run build
